{
  "name": "p58website",
  "version": "1.0.0",
  "private": true,
  "description": "Project58 site + dashboard. Static pages; /api and middleware.js add the dashboard login.",
  "type": "module",
  "scripts": {
    "account": "node scripts/hash-password.mjs",
    "secret": "node scripts/hash-password.mjs --secret"
  },
  "dependencies": {
    "@vercel/functions": "^3.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
