PROJECT58 — TODO
================

[x] Create dashboard accounts
    - Done. Real server-side auth: middleware.js gates the dashboard files at
      the edge, /api/login issues a signed HttpOnly cookie, roles are admin
      (everything) and editor (content only). Setup steps in AUTH_SETUP.md —
      SESSION_SECRET and P58_USERS must be set in Vercel or the dashboard
      stays locked for everyone.

[ ] Projects: add a toggle to hide the projects and display all the categories
    - In the Projects view, a switch that collapses the project rows and
      shows just the list of categories instead.

- In the people page, we can have a part where we will have exterior collaborator. and we need to add in the description somewhere